package usermanagement

import (
	"fmt"
)

//var users = make(map[string]dataTypes.Users)

func HandleUser(userUpdate string) {

	/* TODO:
	/ Take ind the quiz commands/request from the WebSocket and decide what to do and which of the functions to call.
	/
	*/

}

func createUser() {
	action := "CREATE"
	fmt.Printf("You are trying to %s User %s, but the functionality has not yet been implemented!", action)
}
